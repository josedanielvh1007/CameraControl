# CameraControl
Remote control of the laptop by using OpenCV for camera visualization and PyAutoGUI to control the device. The communications are modeled using Ros nodes, initially in Python, then migrating to C++
